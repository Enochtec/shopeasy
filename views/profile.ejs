<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Profile - Enoch's Store</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans min-h-screen flex flex-col">
  <header class="bg-white shadow-md">
    <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-2xl font-bold text-indigo-600">Your Profile</h1>
      <nav>
        <a href="/" class="text-gray-600 hover:text-indigo-600">Home</a>
        <a href="/profile/edit" class="ml-4 text-indigo-600 hover:underline">Edit Profile</a>
      </nav>
    </div>
  </header>

  <main class="max-w-4xl mx-auto py-12 px-4 flex-grow bg-white rounded shadow p-8">
    <dl class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <dt class="font-semibold text-gray-700">Full Name</dt>
        <dd class="mt-1 text-gray-900"><%= user.name %></dd>
      </div>
      <div>
        <dt class="font-semibold text-gray-700">Email</dt>
        <dd class="mt-1 text-gray-900"><%= user.email %></dd>
      </div>
      <div>
        <dt class="font-semibold text-gray-700">Joined On</dt>
        <dd class="mt-1 text-gray-900"><%= new Date(user.createdAt).toLocaleDateString() %></dd>
      </div>
    </dl>
  </main>
</body>
</html>
